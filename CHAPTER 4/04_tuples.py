# creatng a tuple using ()
t = (1, 4, 6, 4)
# r = () empty tuple
r = (1,) #tuple with single elment
print(r)

# Printing the elements of tuple
print(t[0])

# cannot update the value of tuple
# t[0] = 36 throws an error